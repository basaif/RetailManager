<UserControl x:Class="RMDesktopUI.Views.StatusInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RMDesktopUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="20">
            <TextBlock x:Name="Header" FontSize="24" FontWeight="Bold"
                       Margin="0 0 0 10"/>
            <TextBlock x:Name="Message" FontSize="16" FontWeight="Normal"
                       Margin="0 0 0 20"/>
            <Button x:Name="Close" Content="Close" Background="Crimson" Foreground="White"
                    FontWeight="Bold"
                    HorizontalAlignment="Right"
                    Padding="10 5"/>
        </StackPanel>   
    </Grid>
</UserControl>
